<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.aipr.mapper.ReviewCommentMapper">

    <resultMap id="BaseResultMap" type="com.example.aipr.entity.ReviewComment">
        <id column="id" property="id"/>
        <result column="task_id" property="taskId"/>
        <result column="file_path" property="filePath"/>
        <result column="line_number" property="lineNumber"/>
        <result column="risk_type" property="riskType"/>
        <result column="risk_level" property="riskLevel"/>
        <result column="title" property="title"/>
        <result column="description" property="description"/>
        <result column="reason" property="reason"/>
        <result column="suggestion" property="suggestion"/>
        <result column="confidence" property="confidence"/>
        <result column="need_human_check" property="needHumanCheck"/>
        <result column="created_at" property="createdAt"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, task_id, file_path, line_number, risk_type, risk_level,
        title, description, reason, suggestion, confidence, need_human_check, created_at
    </sql>

    <select id="findByTaskId" resultMap="BaseResultMap">
        SELECT * FROM review_comment
        WHERE task_id = #{taskId}
        ORDER BY
            CASE risk_level
                WHEN 'HIGH' THEN 1
                WHEN 'MEDIUM' THEN 2
                WHEN 'LOW' THEN 3
                ELSE 4
            END,
            created_at ASC
    </select>

    <select id="findByTaskIdAndRiskLevel" resultMap="BaseResultMap">
        SELECT * FROM review_comment
        WHERE task_id = #{taskId}
          AND risk_level = #{riskLevel}
        ORDER BY created_at ASC
    </select>

    <select id="countByTaskId" resultType="int">
        SELECT COUNT(*) FROM review_comment WHERE task_id = #{taskId}
    </select>

    <select id="countHighRiskByTaskId" resultType="int">
        SELECT COUNT(*) FROM review_comment
        WHERE task_id = #{taskId}
          AND risk_level = 'HIGH'
    </select>

    <insert id="insertBatch" parameterType="java.util.List">
        INSERT INTO review_comment (task_id, file_path, line_number, risk_type, risk_level,
                                    title, description, reason, suggestion, confidence, need_human_check)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.taskId}, #{item.filePath}, #{item.lineNumber}, #{item.riskType},
             #{item.riskLevel}, #{item.title}, #{item.description}, #{item.reason},
             #{item.suggestion}, #{item.confidence}, #{item.needHumanCheck})
        </foreach>
    </insert>

</mapper>