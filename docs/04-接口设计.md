# AI PR Review 助手接口设计文档

## 1. 文档说明

本文档用于说明「AI PR Review 助手」后端 REST API 的接口设计，包括通用约定、数据结构、接口详情、错误处理和前后端联调说明。

本项目第一版面向 3 天比赛 MVP，接口设计优先服务以下核心链路：

```text
输入 GitHub Pull Request 链接
→ 解析 PR 地址
→ 获取 GitHub PR 信息、提交记录、变更文件和 diff
→ 预处理 diff
→ 执行规则扫描和 AI Review
→ 返回结构化 Review 报告
→ 前端展示 PR 总结、风险识别、Review 建议和测试建议
```

第一版不设计用户登录、权限、多租户、Webhook、自动评论到 GitHub 等复杂功能。

## 2. 接口通用约定

### 2.1 基础路径

所有接口统一以 `/api` 开头。

开发环境后端地址：

```text
http://localhost:8080
```

前端开发环境建议通过 Vite 代理访问后端：

```text
/api -> http://localhost:8080
```

### 2.2 请求格式

接口请求体统一使用 JSON。

请求头：

```http
Content-Type: application/json
Accept: application/json
```

字段命名统一使用 camelCase。

示例：

```json
{
  "prUrl": "https://github.com/owner/repo/pull/12"
}
```

### 2.3 统一响应格式

所有接口统一返回 `Result<T>`。

```json
{
  "code": 0,
  "message": "success",
  "data": {}
}
```

字段说明：

| 字段 | 类型 | 是否必填 | 说明 |
| --- | --- | --- | --- |
| code | number | 是 | 状态码，0 表示成功，非 0 表示失败 |
| message | string | 是 | 提示信息，成功时通常为 `success` |
| data | object / array / null | 否 | 具体业务数据，失败时为 `null` |

成功响应示例：

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "status": "UP"
  }
}
```

### 2.4 错误响应格式

错误响应仍使用统一 `Result<T>` 结构，`code` 不为 0。

```json
{
  "code": 40001,
  "message": "PR 链接不能为空",
  "data": null
}
```

建议 HTTP 状态码：

| HTTP 状态码 | 场景 |
| --- | --- |
| 200 | 请求成功 |
| 400 | 参数错误，例如 PR 链接为空或格式错误 |
| 403 | GitHub API 限流、私有仓库无权限访问 |
| 404 | GitHub 仓库不存在或 PR 不存在 |
| 422 | PR diff 为空或 PR 内容无法分析 |
| 500 | 系统内部异常 |
| 502 | GitHub API 或 AI 服务调用失败 |

前端展示时优先读取 `message` 字段，不直接展示原始异常信息。

## 3. 数据结构说明

### 3.1 ReviewAnalyzeRequest

PR 分析请求对象。

| 字段 | 类型 | 是否必填 | 示例 | 说明 |
| --- | --- | --- | --- | --- |
| prUrl | string | 是 | `https://github.com/owner/repo/pull/12` | GitHub Pull Request 链接 |

校验规则：

| 字段 | 规则 |
| --- | --- |
| prUrl | 不能为空 |
| prUrl | 必须符合 `https://github.com/{owner}/{repo}/pull/{pullNumber}` 格式 |
| pullNumber | 必须为正整数 |

示例：

```json
{
  "prUrl": "https://github.com/owner/repo/pull/12"
}
```

### 3.2 ReviewReport

AI Review 报告对象，是 PR 分析接口的核心返回数据。

| 字段 | 类型 | 是否必填 | 说明 |
| --- | --- | --- | --- |
| taskId | number | 否 | 分析任务 ID，便于后续查询历史和详情 |
| prInfo | PrInfo | 否 | PR 基本信息 |
| summary | string | 否 | PR 总结 |
| riskScore | number | 否 | 风险评分，范围 0 到 100 |
| riskLevel | string | 否 | 风险等级：`LOW`、`MEDIUM`、`HIGH`、`CRITICAL` |
| mainChanges | string[] | 否 | 主要变更列表 |
| riskItems | RiskItem[] | 否 | 风险问题列表 |
| testSuggestions | string[] | 否 | 测试补充建议 |
| finalReview | string | 否 | 最终 Review 结论 |

示例：

```json
{
  "taskId": 1,
  "prInfo": {
    "title": "fix login token bug",
    "author": "demo-user",
    "url": "https://github.com/owner/repo/pull/12",
    "sourceBranch": "feature/login-token",
    "targetBranch": "main",
    "changedFiles": 4,
    "additions": 120,
    "deletions": 35
  },
  "summary": "本次 PR 主要修改了登录认证逻辑，新增 token 校验并调整用户接口返回结构。",
  "riskScore": 78,
  "riskLevel": "HIGH",
  "mainChanges": [
    "新增 JwtUtil 工具类",
    "修改 LoginService 登录逻辑",
    "调整 UserController 返回结构"
  ],
  "riskItems": [
    {
      "filePath": "src/main/java/com/demo/auth/JwtUtil.java",
      "lineNumber": 35,
      "riskLevel": "HIGH",
      "riskType": "SECURITY_RISK",
      "title": "JWT 密钥存在硬编码风险",
      "description": "密钥直接写在源码中，公开仓库可能导致泄露",
      "reason": "密钥直接写在源码中，公开仓库或日志泄露时可能导致 token 被伪造。",
      "suggestion": "建议改为从环境变量或安全配置中读取，并避免在代码库中提交真实密钥。",
      "confidence": 0.93,
      "needHumanCheck": false
    }
  ],
  "testSuggestions": [
    "建议补充 token 过期场景测试",
    "建议补充登录失败场景测试"
  ],
  "finalReview": "建议修改高风险问题后再合并。"
}
```

### 3.3 PrInfo

PR 基本信息对象。

| 字段 | 类型 | 是否必填 | 说明 |
| --- | --- | --- | --- |
| title | string | 否 | PR 标题 |
| author | string | 否 | PR 作者 GitHub 登录名 |
| url | string | 否 | PR 页面链接 |
| sourceBranch | string | 否 | 源分支 |
| targetBranch | string | 否 | 目标分支 |
| changedFiles | number | 否 | 变更文件数量 |
| additions | number | 否 | 新增行数 |
| deletions | number | 否 | 删除行数 |

示例：

```json
{
  "title": "fix login token bug",
  "author": "demo-user",
  "url": "https://github.com/owner/repo/pull/12",
  "sourceBranch": "feature/login-token",
  "targetBranch": "main",
  "changedFiles": 4,
  "additions": 120,
  "deletions": 35
}
```

### 3.4 RiskItem

单条风险问题对象。

| 字段 | 类型 | 是否必填 | 说明 |
| --- | --- | --- | --- |
| filePath | string | 否 | 风险所在文件路径 |
| lineNumber | number | 否 | 代码行号 |
| riskLevel | string | 否 | 风险等级：`LOW`、`MEDIUM`、`HIGH`、`CRITICAL` |
| riskType | string | 否 | 风险类型 |
| title | string | 否 | 问题标题 |
| description | string | 否 | 问题描述 |
| reason | string | 否 | 风险原因说明 |
| suggestion | string | 否 | 修改建议 |
| confidence | number | 否 | 置信度：0 到 1 之间的小数 |
| needHumanCheck | boolean | 否 | 是否需要人工确认 |

示例：

```json
{
  "filePath": "src/main/java/com/demo/auth/JwtUtil.java",
  "lineNumber": 35,
  "riskLevel": "HIGH",
  "riskType": "SECURITY_RISK",
  "title": "JWT 密钥存在硬编码风险",
  "description": "密钥直接写在源码中，公开仓库可能导致泄露",
  "reason": "密钥直接写在源码中，公开仓库或日志泄露时可能导致 token 被伪造。",
  "suggestion": "建议改为从环境变量或安全配置中读取，并避免在代码库中提交真实密钥。",
  "confidence": 0.93,
  "needHumanCheck": false
}
```

### 3.5 ReviewHistoryItem

历史分析记录列表项。该对象用于可选历史查询接口。

| 字段 | 类型 | 是否必填 | 说明 |
| --- | --- | --- | --- |
| taskId | number | 是 | 分析任务 ID |
| prUrl | string | 是 | PR 链接 |
| prTitle | string | 否 | PR 标题 |
| riskScore | number | 否 | 风险评分 |
| riskLevel | string | 否 | 风险等级 |
| createdAt | string | 否 | 创建时间，建议格式为 `yyyy-MM-dd HH:mm:ss` |

示例：

```json
{
  "taskId": 1,
  "prUrl": "https://github.com/owner/repo/pull/12",
  "prTitle": "fix login token bug",
  "riskScore": 78,
  "riskLevel": "HIGH",
  "createdAt": "2026-05-29 15:30:00"
}
```

## 4. 接口详情

### 4.1 健康检查接口

接口说明：

用于检查后端服务是否正常运行。前端启动前、部署后或比赛演示前均可调用。

请求方式：

```http
GET /api/health
```

请求参数：

无。

请求示例：

```http
GET http://localhost:8080/api/health
```

响应字段：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| status | string | 服务状态，正常时为 `UP` |
| service | string | 服务名称 |
| timestamp | string | 服务当前时间 |

响应示例：

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "status": "UP",
    "service": "ai-pr-review-backend",
    "timestamp": "2026-05-29 15:30:00"
  }
}
```

异常响应示例：

```json
{
  "code": 50000,
  "message": "服务暂不可用",
  "data": null
}
```

### 4.2 PR 分析接口

接口说明：

接收 GitHub Pull Request 链接，触发 PR 分析流程，并返回 AI Review 报告。

第一版 MVP 建议采用同步接口，便于前端联调和比赛演示。后续如果分析耗时较长，可以扩展为异步任务模式。

请求方式：

```http
POST /api/review-tasks
```

请求体：

| 字段 | 类型 | 是否必填 | 说明 |
| --- | --- | --- | --- |
| prUrl | string | 是 | GitHub Pull Request 链接 |

请求示例：

```json
{
  "prUrl": "https://github.com/owner/repo/pull/12"
}
```

处理流程：

```text
1. 校验 prUrl 是否为空
2. 校验 prUrl 是否符合 GitHub PR 链接格式
3. 解析 owner、repo、pullNumber
4. 调用 GitHub API 获取 PR 基本信息
5. 调用 GitHub API 获取提交记录、变更文件和 diff
6. 对 diff 进行过滤、截断或摘要处理
7. 执行规则扫描
8. 调用大模型生成 Review 报告
9. 解析 AI 返回 JSON
10. 返回 ReviewReport
```

响应字段：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| taskId | number | 分析任务 ID |
| prInfo | PrInfo | PR 基本信息 |
| summary | string | PR 总结 |
| riskScore | number | 风险评分 |
| riskLevel | string | 风险等级 |
| mainChanges | string[] | 主要变更列表 |
| riskItems | RiskItem[] | 风险问题列表 |
| testSuggestions | string[] | 测试建议列表 |
| finalReview | string | 最终 Review 结论 |

成功响应示例：

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "taskId": 1,
    "prInfo": {
      "title": "fix login token bug",
      "author": "demo-user",
      "url": "https://github.com/owner/repo/pull/12",
      "sourceBranch": "feature/login-token",
      "targetBranch": "main",
      "changedFiles": 4,
      "additions": 120,
      "deletions": 35
    },
    "summary": "本次 PR 主要修改了登录认证逻辑，新增 token 校验并调整用户接口返回结构。",
    "riskScore": 78,
    "riskLevel": "HIGH",
    "mainChanges": [
      "新增 JwtUtil 工具类",
      "修改 LoginService 登录逻辑",
      "调整 UserController 返回结构"
    ],
    "riskItems": [
      {
        "filePath": "src/main/java/com/demo/auth/JwtUtil.java",
        "lineNumber": 35,
        "riskLevel": "HIGH",
        "riskType": "SECURITY_RISK",
        "title": "JWT 密钥存在硬编码风险",
        "description": "密钥直接写在源码中，公开仓库可能导致泄露",
        "reason": "密钥直接写在源码中，公开仓库或日志泄露时可能导致 token 被伪造。",
        "suggestion": "建议改为从环境变量或安全配置中读取，并避免在代码库中提交真实密钥。",
        "confidence": 0.93,
        "needHumanCheck": false
      },
      {
        "filePath": "src/main/java/com/demo/service/LoginService.java",
        "lineNumber": 42,
        "riskLevel": "MEDIUM",
        "riskType": "BUG_RISK",
        "title": "登录失败场景缺少明确异常处理",
        "description": "当前逻辑可能返回空结果，前端无法区分用户不存在和密码错误",
        "reason": "当前逻辑可能返回空结果，前端无法区分用户不存在和密码错误",
        "suggestion": "建议使用统一业务异常并返回清晰错误信息",
        "confidence": 0.75,
        "needHumanCheck": true
      }
    ],
    "testSuggestions": [
      "建议补充 token 过期场景测试",
      "建议补充登录失败场景测试"
    ],
    "finalReview": "建议修改高风险问题后再合并。"
  }
}
```

参数错误响应示例：

```json
{
  "code": 40001,
  "message": "PR 链接格式错误，请输入 GitHub Pull Request 地址",
  "data": null
}
```

GitHub API 异常响应示例：

```json
{
  "code": 40401,
  "message": "GitHub 仓库不存在或 PR 不存在",
  "data": null
}
```

AI 服务异常响应示例：

```json
{
  "code": 50201,
  "message": "AI 服务调用失败，请稍后重试",
  "data": null
}
```

AI 返回格式异常响应示例：

```json
{
  "code": 50202,
  "message": "AI 返回格式解析失败，请重新分析",
  "data": null
}
```

### 4.3 查询分析历史接口，可选

接口说明：

查询最近的 PR 分析记录，用于首页历史记录或演示时快速回看结果。

该接口属于可选功能。如果第一版只实现同步分析且不保存历史记录，可以暂不实现。

请求方式：

```http
GET /api/reviews/history
```

查询参数：

| 参数 | 类型 | 是否必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| limit | number | 否 | 10 | 返回最近多少条记录，建议最大 50 |

请求示例：

```http
GET http://localhost:8080/api/reviews/history?limit=10
```

响应数据：

返回 `ReviewHistoryItem[]`。

成功响应示例：

```json
{
  "code": 0,
  "message": "success",
  "data": [
    {
      "taskId": 1,
      "prUrl": "https://github.com/owner/repo/pull/12",
      "prTitle": "fix login token bug",
      "riskScore": 78,
      "riskLevel": "HIGH",
      "createdAt": "2026-05-29 15:30:00"
    },
    {
      "taskId": 2,
      "prUrl": "https://github.com/owner/repo/pull/13",
      "prTitle": "refactor user profile api",
      "riskScore": 42,
      "riskLevel": "MEDIUM",
      "createdAt": "2026-05-29 16:10:00"
    }
  ]
}
```

空数据响应示例：

```json
{
  "code": 0,
  "message": "success",
  "data": []
}
```

异常响应示例：

```json
{
  "code": 50001,
  "message": "查询分析历史失败",
  "data": null
}
```

### 4.4 查询分析详情接口，可选

接口说明：

根据任务 ID 查询某次 PR 分析详情。适用于历史记录详情页、页面刷新恢复报告、后续异步任务查询等场景。

如果第一版采用同步分析且不保存结果，可以暂不实现该接口。

请求方式：

```http
GET /api/reviews/{taskId}
```

路径参数：

| 参数 | 类型 | 是否必填 | 说明 |
| --- | --- | --- | --- |
| taskId | number | 是 | 分析任务 ID |

请求示例：

```http
GET http://localhost:8080/api/reviews/1
```

成功响应示例：

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "taskId": 1,
    "prInfo": {
      "title": "fix login token bug",
      "author": "demo-user",
      "url": "https://github.com/owner/repo/pull/12",
      "sourceBranch": "feature/login-token",
      "targetBranch": "main",
      "changedFiles": 4,
      "additions": 120,
      "deletions": 35
    },
    "summary": "本次 PR 主要修改了登录认证逻辑，新增 token 校验并调整用户接口返回结构。",
    "riskScore": 78,
    "riskLevel": "HIGH",
    "mainChanges": [
      "新增 JwtUtil 工具类",
      "修改 LoginService 登录逻辑",
      "调整 UserController 返回结构"
    ],
    "riskItems": [
      {
        "filePath": "src/main/java/com/demo/auth/JwtUtil.java",
        "lineNumber": 35,
        "riskLevel": "HIGH",
        "riskType": "SECURITY_RISK",
        "title": "JWT 密钥存在硬编码风险",
        "description": "密钥直接写在源码中，公开仓库可能导致泄露",
        "reason": "密钥直接写在源码中，公开仓库或日志泄露时可能导致 token 被伪造。",
        "suggestion": "建议改为从环境变量或安全配置中读取，并避免在代码库中提交真实密钥。",
        "confidence": 0.93,
        "needHumanCheck": false
      }
    ],
    "testSuggestions": [
      "建议补充 token 过期场景测试",
      "建议补充登录失败场景测试"
    ],
    "finalReview": "建议修改高风险问题后再合并。"
  }
}
```

任务不存在响应示例：

```json
{
  "code": 40401,
  "message": "分析记录不存在",
  "data": null
}
```

## 5. 风险等级与风险类型说明

### 5.1 风险等级

| 风险等级 | 含义 | 展示建议 |
| --- | --- | --- |
| LOW | 低风险，通常是可维护性、测试补充或小范围优化建议 | 绿色 |
| MEDIUM | 中风险，建议开发者优先检查，可能影响异常流程或边界场景 | 橙色 |
| HIGH | 高风险，可能导致安全问题、线上故障或核心流程不可用 | 红色 |
| CRITICAL | 严重风险，可能导致严重安全问题、数据破坏或核心链路不可用 | 深红色 |

最终 `riskScore` 和 `riskLevel` 由后端 `RiskScoreCalculator` 基于已保存的 Review Comment 固定规则计算，不直接采用 AI 输出的总评分。

| 分数范围 | 风险等级 |
| --- | --- |
| 0 - 30 | LOW |
| 31 - 60 | MEDIUM |
| 61 - 85 | HIGH |
| 86 - 100 | CRITICAL |

### 5.2 风险类型

| 风险类型 | 含义 | 示例 |
| --- | --- | --- |
| BUG_RISK | 可能导致功能错误、空指针、异常流程或数据错误 | 变量未初始化、数组越界 |
| SECURITY_RISK | 安全相关风险，包括密钥、认证、权限、注入、敏感信息 | 硬编码密钥、SQL 注入、权限绕过 |
| PERFORMANCE_RISK | 性能风险，例如循环查询、无分页、大对象处理 | 循环内远程调用、重复查询 |
| MAINTAINABILITY | 可维护性风险，例如复杂逻辑、重复代码、边界不清晰 | 过长方法、重复代码 |
| STYLE | 代码风格、命名、格式等非阻塞问题 | 命名不规范、格式不统一 |
| TEST_RISK | 测试缺失、测试覆盖不足、关键场景未验证 | 核心逻辑缺少单元测试 |
| COMPATIBILITY | 兼容性风险，例如接口契约、依赖版本、配置变更影响 | API 契约变更未同步 |

说明：新增风险类型时，需要同步更新 Prompt、后端枚举、前端展示和本文档。

## 6. 常见错误说明

| 错误场景 | 建议 HTTP 状态码 | message 示例 | 前端处理建议 |
| --- | --- | --- | --- |
| PR 链接为空 | 400 | PR 链接不能为空 | 提示用户输入 PR 链接 |
| PR 链接格式错误 | 400 | PR 链接格式错误，请输入 GitHub Pull Request 地址 | 展示正确格式示例 |
| GitHub 仓库不存在 | 404 | GitHub 仓库不存在 | 提示检查 owner 和 repo |
| PR 不存在 | 404 | PR 不存在 | 提示检查 PR 编号 |
| GitHub API 限流 | 403 | GitHub API 限流，请稍后重试 | 提示稍后重试或检查 Token |
| 私有仓库无权限访问 | 403 | GitHub 仓库无权限访问，请检查 Token | 提示检查 GitHub Token 权限 |
| PR diff 为空 | 422 | PR diff 为空，暂无可分析内容 | 展示空状态 |
| PR diff 过大 | 200 / 422 | PR diff 过大，仅分析部分内容 | MVP 建议截断并在报告中提示 |
| AI 服务调用失败 | 502 | AI 服务调用失败，请稍后重试 | 提示稍后重试 |
| AI 返回格式解析失败 | 502 | AI 返回格式解析失败，请重新分析 | 提示重新分析 |
| 系统内部异常 | 500 | 系统内部异常，请稍后重试 | 展示通用错误提示 |

错误响应统一示例：

```json
{
  "code": 50000,
  "message": "AI 服务调用失败，请稍后重试",
  "data": null
}
```

## 7. 前后端联调说明

### 7.1 前端请求地址

前端统一请求相对路径：

```text
/api/review-tasks
```

不要在前端代码中写死后端完整地址。

### 7.2 Vite 代理配置

开发环境建议在 `frontend/vite.config.js` 中配置：

```js
server: {
  proxy: {
    '/api': {
      target: 'http://localhost:8080',
      changeOrigin: true
    }
  }
}
```

### 7.3 前端兼容策略

前端需要兼容以下情况：

1. 后端未启动：提示 `AI 分析失败，请稍后重试`。
2. `success=false`：展示后端返回的 `message`。
3. `data.prInfo` 缺失：展示默认占位，不让页面崩溃。
4. `riskItems` 为空：展示 `暂无风险建议，可能该 PR 风险较低`。
5. `testSuggestions` 为空：展示 `暂无测试建议。`。
6. 分析耗时较长：按钮展示 loading，避免重复提交。

### 7.4 推荐联调流程

```text
1. 启动 MySQL
2. 启动后端服务
3. 调用 GET /api/health 确认服务正常
4. 启动前端服务
5. 使用 Mock 数据确认页面展示正常
6. 关闭 Mock 开关，输入真实 GitHub PR 链接
7. 调用 POST /api/review-tasks
8. 检查 PR 基本信息、风险评分、风险列表和测试建议是否正常展示
9. 测试错误 PR 链接、无权限仓库、AI 服务异常等错误提示
```

### 7.5 PR 链接格式

当前仅支持 GitHub Pull Request 链接：

```text
https://github.com/{owner}/{repo}/pull/{pullNumber}
```

示例：

```text
https://github.com/owner/repo/pull/12
```

暂不支持 GitLab、Gitee、commit 链接、compare 链接和 issue 链接。

## 8. 升级方案接口设计

本节用于承接后续“PR 列表工作台 + 任务中心 + 报告详情 + 扩展监控”的升级计划。接口仍统一使用 `/api` 前缀和 `Result<T>` 响应结构。

### 8.1 分页响应结构

新增列表类接口统一使用 `PageVO<T>`：

```json
{
  "records": [],
  "page": 1,
  "pageSize": 10,
  "total": 42,
  "pages": 5
}
```

字段说明：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| records | array | 当前页数据 |
| page | number | 当前页，从 1 开始 |
| pageSize | number | 每页条数 |
| total | number | 总记录数，GitHub 列表类接口可为 null |
| pages | number | 总页数，GitHub 列表类接口可为 null |

默认分页参数：

```text
page=1
pageSize=10
```

约束：

```text
page >= 1
1 <= pageSize <= 50
```

### 8.2 查询仓库 PR 列表

用于把首页从“手动输入 PR 链接”升级为“选择仓库 PR 后开始评审”。

```http
GET /api/github/pulls
```

当前状态：后端已实现。该接口只获取 PR 列表和本地评审状态，不拉取 changed files 和 diff，避免工作台列表页响应过慢。

Query 参数：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| owner | string | 是 | 无 | 仓库 owner，例如 `crash73` |
| repo | string | 是 | 无 | 仓库名，例如 `test-for-PRreview` |
| state | string | 否 | `open` | `open`、`closed`、`all` |
| page | number | 否 | `1` | 当前页 |
| pageSize | number | 否 | `10` | 每页数量，最大 50 |

示例：

```http
GET /api/github/pulls?owner=crash73&repo=test-for-PRreview&state=open&page=1&pageSize=10
```

后端调用 GitHub API：

```http
GET /repos/{owner}/{repo}/pulls?state=open&per_page=10&page=1
```

响应：

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "records": [
      {
        "owner": "crash73",
        "repo": "test-for-PRreview",
        "pullNumber": 1,
        "title": "feat: 添加认证服务演示变更",
        "author": "crash73",
        "state": "OPEN",
        "sourceBranch": "feature/review-demo",
        "targetBranch": "main",
        "htmlUrl": "https://github.com/crash73/test-for-PRreview/pull/1",
        "createdAt": "2026-05-30 19:30:00",
        "updatedAt": "2026-05-30 19:35:00",
        "draft": false,
        "reviewed": true,
        "latestTaskId": 12,
        "latestTaskStatus": "SUCCESS",
        "latestRiskLevel": "HIGH",
        "latestRiskScore": 78,
        "cachedAvailable": true
      }
    ],
    "page": 1,
    "pageSize": 10,
    "total": null,
    "pages": null
  }
}
```

说明：

1. GitHub PR 列表接口本身不返回稳定 total，第一版允许 `total/pages` 为 `null`。
2. `reviewed/latestTaskId/latestRiskLevel/cachedAvailable` 由本地 `review_task` 表补充。
3. 第一版不强制返回 `additions/deletions/changedFiles`，避免列表页对每个 PR 额外请求详情。
4. 点击“开始评审”时继续复用 `POST /api/review-tasks`。

### 8.3 查询单个 PR 本地评审状态

用于 PR 卡片刷新当前 PR 是否已有历史 Review。

```http
GET /api/github/pulls/{owner}/{repo}/{pullNumber}/review-state
```

当前状态：后端已实现。接口会查询 GitHub 当前 PR head sha，并结合本地 `review_task` 判断是否已有历史评审与可复用缓存。

响应：

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "owner": "crash73",
    "repo": "test-for-PRreview",
    "pullNumber": 1,
    "reviewed": true,
    "latestTaskId": 12,
    "latestTaskStatus": "SUCCESS",
    "latestRiskLevel": "HIGH",
    "latestRiskScore": 78,
    "cachedAvailable": true,
    "updatedAt": "2026-05-30 20:10:00"
  }
}
```

优先级：

```text
P1。P0 可以先只做 /api/github/pulls。
```

### 8.4 查询 Review 任务列表

用于任务中心、最近任务和历史报告列表。

```http
GET /api/review-tasks
```

Query 参数：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| page | number | 否 | `1` | 当前页 |
| pageSize | number | 否 | `10` | 每页数量 |
| status | string | 否 | 无 | `PENDING`、`REVIEWING`、`SUCCESS`、`FAILED` 等 |
| riskLevel | string | 否 | 无 | `LOW`、`MEDIUM`、`HIGH`、`CRITICAL` |
| owner | string | 否 | 无 | 仓库 owner |
| repo | string | 否 | 无 | 仓库名 |
| keyword | string | 否 | 无 | 按 PR 标题、仓库、作者模糊搜索 |
| cached | boolean | 否 | 无 | 是否为缓存命中任务 |
| createdFrom | string | 否 | 无 | 创建开始时间，格式 `yyyy-MM-dd HH:mm:ss` |
| createdTo | string | 否 | 无 | 创建结束时间，格式 `yyyy-MM-dd HH:mm:ss` |

示例：

```http
GET /api/review-tasks?page=1&pageSize=10&status=SUCCESS&riskLevel=HIGH&keyword=auth
```

响应：

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "records": [
      {
        "taskId": 12,
        "prUrl": "https://github.com/crash73/test-for-PRreview/pull/1",
        "ownerName": "crash73",
        "repoName": "test-for-PRreview",
        "pullNumber": 1,
        "prTitle": "feat: 添加认证服务演示变更",
        "author": "crash73",
        "sourceBranch": "feature/review-demo",
        "targetBranch": "main",
        "status": "SUCCESS",
        "riskScore": 78,
        "riskLevel": "HIGH",
        "modelName": "deepseek-chat",
        "promptVersion": "v1",
        "cached": false,
        "cachedFromTaskId": null,
        "createdAt": "2026-05-30 20:01:00",
        "updatedAt": "2026-05-30 20:03:00",
        "errorMessage": null
      }
    ],
    "page": 1,
    "pageSize": 10,
    "total": 1,
    "pages": 1
  }
}
```

### 8.5 创建 Review 任务的缓存约定

当前接口已实现，升级后继续作为 PR 工作台的核心动作。

```http
POST /api/review-tasks
```

请求：

```json
{
  "prUrl": "https://github.com/crash73/test-for-PRreview/pull/1",
  "forceRefresh": false
}
```

响应：

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "taskId": 12,
    "status": "SUCCESS",
    "cached": true,
    "cachedFromTaskId": 12
  }
}
```

说明：

1. `forceRefresh=false` 时，后端优先查缓存。
2. 命中缓存时直接返回历史成功任务 `taskId`，前端不需要长时间轮询。
3. `forceRefresh=true` 时，忽略缓存并创建新任务。
4. 前端重新分析按钮必须传 `forceRefresh=true`。

### 8.6 查询 Review 任务详情

当前已实现。

```http
GET /api/review-tasks/{taskId}
```

升级后建议补充字段：

```json
{
  "taskId": 12,
  "prUrl": "https://github.com/crash73/test-for-PRreview/pull/1",
  "prTitle": "feat: 添加认证服务演示变更",
  "author": "crash73",
  "sourceBranch": "feature/review-demo",
  "targetBranch": "main",
  "status": "SUCCESS",
  "riskScore": 78,
  "riskLevel": "HIGH",
  "modelName": "deepseek-chat",
  "promptVersion": "v1",
  "cached": false,
  "cachedFromTaskId": null,
  "errorMessage": null,
  "createdAt": "2026-05-30 20:01:00",
  "updatedAt": "2026-05-30 20:03:00"
}
```

### 8.7 查询报告总览

当前已实现。

```http
GET /api/review-tasks/{taskId}/report
```

响应核心结构：

```json
{
  "taskId": 12,
  "prInfo": {
    "title": "feat: 添加认证服务演示变更",
    "author": "crash73",
    "url": "https://github.com/crash73/test-for-PRreview/pull/1",
    "sourceBranch": "feature/review-demo",
    "targetBranch": "main",
    "changedFiles": 2,
    "additions": 66,
    "deletions": 0
  },
  "summary": "本次 PR 新增认证服务逻辑。",
  "riskScore": 78,
  "riskLevel": "HIGH",
  "mainChanges": [],
  "riskItems": [],
  "testSuggestions": [],
  "finalReview": "建议修复高风险问题后再合并。"
}
```

### 8.8 查询报告文件视图

当前已有文件列表接口，升级为报告详情页文件 Tab 数据源。

```http
GET /api/review-tasks/{taskId}/files
```

响应：

```json
{
  "code": 0,
  "message": "success",
  "data": [
    {
      "fileId": 31,
      "taskId": 12,
      "filePath": "src/main/java/com/crash73/prreview/AuthService.java",
      "fileStatus": "added",
      "language": "Java",
      "additions": 28,
      "deletions": 0,
      "changes": 28,
      "aiSummary": "该文件新增登录认证逻辑。",
      "skipped": false,
      "skipReason": null,
      "commentCount": 4,
      "highestRiskLevel": "HIGH"
    }
  ]
}
```

说明：

```text
commentCount/highestRiskLevel 当前可以前端按 comments 聚合，也可以后端后续补充。
```

### 8.9 查询 Review 建议列表

当前已实现。

```http
GET /api/review-tasks/{taskId}/comments
```

Query 参数：

| 参数 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| riskLevel | string | 否 | 按风险等级过滤 |
| riskType | string | 否 | 按风险类型过滤 |
| filePath | string | 否 | 后续可加，按文件过滤 |

响应：

```json
{
  "code": 0,
  "message": "success",
  "data": [
    {
      "id": 101,
      "taskId": 12,
      "filePath": "src/main/java/com/crash73/prreview/AuthService.java",
      "line": 8,
      "riskType": "SECURITY_RISK",
      "riskLevel": "HIGH",
      "title": "Token Secret 存在硬编码风险",
      "description": "密钥直接写在源码中。",
      "reason": "仓库泄露或日志暴露时会导致 token 可被伪造。",
      "suggestion": "建议改为从环境变量或安全配置读取。",
      "confidence": 0.92,
      "needHumanCheck": true
    }
  ]
}
```

### 8.10 导出 GitHub Review Markdown

用于“复制 GitHub Review 评论”按钮。

```http
GET /api/review-tasks/{taskId}/review-markdown
```

当前 dev 状态：前端报告页已调用该接口，并在接口缺失时使用 `report/comments` 本地拼装 Markdown 作为兜底；后端仍需补齐，保证后续自动评论到 GitHub PR 时复用同一份稳定文本。

响应：

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "taskId": 12,
    "markdown": "## AI Review 总结\n\n风险等级：HIGH\n..."
  }
}
```

说明：

1. 第一版也可以由前端根据 report/comments 拼 Markdown。
2. 后端导出更稳定，便于后续自动评论到 GitHub PR。
3. 不自动调用 GitHub 评论 API，避免权限和误发风险。

### 8.11 查询系统配置状态

用于 Settings 页和联调前检查。

```http
GET /api/config/status
```

响应：

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "githubTokenConfigured": true,
    "githubApiReachable": true,
    "aiKeyConfigured": true,
    "aiBaseUrl": "https://api.deepseek.com",
    "aiModelName": "deepseek-chat",
    "promptVersion": "v1",
    "databaseConnected": true,
    "cacheEnabled": true
  }
}
```

安全要求：

1. 不返回 `GITHUB_TOKEN`。
2. 不返回 `AI_API_KEY`。
3. 不返回 `DB_PASSWORD`。
4. 只返回是否配置和脱敏后的基础信息。

### 8.12 查询模型用量概览

该部分需要新增 `model_usage_log` 表。若时间紧，可以先不实现，只保留接口设计。

```http
GET /api/model-usage/summary
```

Query 参数：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| from | string | 否 | 今日 00:00 | 开始时间 |
| to | string | 否 | 当前时间 | 结束时间 |
| modelName | string | 否 | 无 | 模型名 |

响应：

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "callCount": 18,
    "successCount": 17,
    "failedCount": 1,
    "successRate": 0.94,
    "promptTokens": 12345,
    "completionTokens": 4321,
    "totalTokens": 16666,
    "avgLatencyMs": 1830
  }
}
```

### 8.13 查询模型调用明细

```http
GET /api/model-usage/logs
```

Query 参数：

| 参数 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| page | number | 否 | 当前页 |
| pageSize | number | 否 | 每页数量 |
| taskId | number | 否 | 按任务过滤 |
| success | boolean | 否 | 是否成功 |
| modelName | string | 否 | 模型名 |

响应：

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "records": [
      {
        "id": 1,
        "taskId": 12,
        "fileId": 31,
        "provider": "openai-compatible",
        "modelName": "deepseek-chat",
        "callType": "FILE_REVIEW",
        "promptTokens": 1200,
        "completionTokens": 360,
        "totalTokens": 1560,
        "latencyMs": 1800,
        "success": true,
        "errorMessage": null,
        "createdAt": "2026-05-30 20:03:00"
      }
    ],
    "page": 1,
    "pageSize": 10,
    "total": 1,
    "pages": 1
  }
}
```

### 8.14 查询任务模型用量

用于报告详情页展示单个任务的 AI 调用统计。

```http
GET /api/model-usage/tasks/{taskId}
```

路径参数：

| 参数 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| taskId | number | 是 | 任务 ID |

响应：

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "taskId": 12,
    "totalCalls": 5,
    "successCalls": 5,
    "failedCalls": 0,
    "totalPromptTokens": 8500,
    "totalCompletionTokens": 2100,
    "totalTokens": 10600,
    "avgLatencyMs": 1850
  }
}
```

字段说明：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| taskId | number | 任务 ID |
| totalCalls | number | 总调用次数 |
| successCalls | number | 成功调用次数 |
| failedCalls | number | 失败调用次数 |
| totalPromptTokens | number | 总 Prompt Token 数 |
| totalCompletionTokens | number | 总 Completion Token 数 |
| totalTokens | number | 总 Token 数 |
| avgLatencyMs | number | 平均耗时（毫秒） |

### 8.15 查询模型用量概览（补充字段）

`GET /api/model-usage/summary` 响应补充以下字段：

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "totalCalls": 18,
    "successCalls": 17,
    "failedCalls": 1,
    "successRate": 94.44,
    "totalPromptTokens": 12345,
    "totalCompletionTokens": 4321,
    "totalTokens": 16666,
    "avgLatencyMs": 1830
  }
}
```

字段说明：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| totalCalls | number | 总调用次数 |
| successCalls | number | 成功调用次数 |
| failedCalls | number | 失败调用次数 |
| successRate | number | 成功率（百分比） |
| totalPromptTokens | number | 总 Prompt Token 数 |
| totalCompletionTokens | number | 总 Completion Token 数 |
| totalTokens | number | 总 Token 数 |
| avgLatencyMs | number | 平均耗时（毫秒） |

### 8.16 查询缓存统计

```http
GET /api/review-cache/statistics
```

响应：

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "totalTasks": 100,
    "cachedTasks": 35,
    "cacheHitRate": 35.0,
    "totalSavedCalls": 52
  }
}
```

字段说明：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| totalTasks | number | 总任务数 |
| cachedTasks | number | 命中缓存的任务数 |
| cacheHitRate | number | 缓存命中率（百分比） |
| totalSavedCalls | number | 节省的 AI 调用次数 |

### 8.17 查询 Skill 列表

当前数据库已有 `review_skill` 和 `review_skill_result` 相关结构，后续可逐步补接口。

```http
GET /api/skills
```

响应：

```json
{
  "code": 0,
  "message": "success",
  "data": [
    {
      "id": 1,
      "skillCode": "JAVA_REVIEW",
      "skillName": "Java 代码评审",
      "skillType": "FILE_REVIEW",
      "description": "面向 Java/Spring 代码的专项评审",
      "supportedLanguages": "Java",
      "enabled": true,
      "priority": 20
    }
  ]
}
```

### 8.15 更新 Skill 启用状态

```http
PATCH /api/skills/{skillCode}/enabled
```

请求：

```json
{
  "enabled": true
}
```

响应：

```json
{
  "code": 0,
  "message": "success",
  "data": true
}
```

### 8.16 查询任务 Skill 执行结果

```http
GET /api/review-tasks/{taskId}/skill-results
```

响应：

```json
{
  "code": 0,
  "message": "success",
  "data": [
    {
      "id": 1,
      "taskId": 12,
      "fileId": 31,
      "filePath": "src/main/java/com/crash73/prreview/AuthService.java",
      "skillCode": "SECURITY_REVIEW",
      "skillName": "安全专项评审",
      "phase": "FILE_REVIEW",
      "success": true,
      "summary": "发现硬编码密钥和敏感日志输出。",
      "errorMessage": null,
      "createdAt": "2026-05-30 20:03:00"
    }
  ]
}
```

安全要求：

```text
rawOutput 默认不返回前端，如需排查只在受控调试接口或后端日志摘要中使用。
```

### 8.17 查询任务执行轨迹

当前不建议直接实现完整 Agent，但前端可以预留执行轨迹 Tab。

```http
GET /api/review-tasks/{taskId}/trace
```

响应：

```json
{
  "code": 0,
  "message": "success",
  "data": [
    {
      "step": "CREATE_TASK",
      "title": "创建任务",
      "status": "SUCCESS",
      "startedAt": "2026-05-30 20:01:00",
      "finishedAt": "2026-05-30 20:01:01",
      "message": "Review 任务已创建"
    },
    {
      "step": "FETCH_PR",
      "title": "获取 PR 信息",
      "status": "SUCCESS",
      "startedAt": "2026-05-30 20:01:01",
      "finishedAt": "2026-05-30 20:01:02",
      "message": "已获取 PR 标题、分支和作者"
    },
    {
      "step": "AI_REVIEW",
      "title": "执行 AI Review",
      "status": "SUCCESS",
      "startedAt": "2026-05-30 20:01:03",
      "finishedAt": "2026-05-30 20:02:30",
      "message": "已完成 2 个文件评审"
    }
  ]
}
```

说明：

1. 第一版可由 `review_task.status` 推导固定 Pipeline，不一定落表。
2. 后续 Agent 化后再存 `Plan / Action / Observation / Final`。

### 8.18 前端 API 文件规划

`frontend/src/api/github.js`：

```js
export async function listPullRequests(params) {}
export async function getPullRequestReviewState(owner, repo, pullNumber) {}
```

`frontend/src/api/review.js`：

```js
export async function createReviewTask(prUrl, forceRefresh = false) {}
export async function listReviewTasks(params) {}
export async function getReviewTask(taskId) {}
export async function getReviewReport(taskId) {}
export async function getReviewFiles(taskId) {}
export async function getReviewComments(taskId, params) {}
export async function getReviewMarkdown(taskId) {}
```

`frontend/src/api/config.js`：

```js
export async function getConfigStatus() {}
```

`frontend/src/api/modelUsage.js`：

```js
export async function getModelUsageSummary(params) {}
export async function listModelUsageLogs(params) {}
```

`frontend/src/api/skill.js`：

```js
export async function listSkills() {}
export async function updateSkillEnabled(skillCode, enabled) {}
export async function listSkillResults(taskId) {}
```

### 8.19 落地优先级

当前 dev 已具备：

```text
1. POST /api/review-tasks 创建评审任务。
2. GET /api/github/pulls，支撑 PR 工作台真实列表。
3. GET /api/github/pulls/{owner}/{repo}/{pullNumber}/review-state，支撑 PR 卡片本地评审状态。
4. GET /api/review-tasks 任务中心列表。
5. GET /api/review-tasks/{taskId} 任务详情和轮询。
6. GET /api/review-tasks/{taskId}/files、comments、report 报告详情数据。
7. GET /api/config/status 配置状态。
8. GET /api/model-usage/summary、logs、tasks/{taskId} 模型用量监控。
9. GET /api/review-tasks/statistics、recent-failures 和 /api/review-cache/statistics 监控统计。
10. 前端 PR 工作台、任务中心、报告页、设置页、模型用量页页面骨架。
```

当前 dev 待补齐：

```text
1. GET /api/review-tasks/{taskId}/review-markdown，统一 GitHub Review Markdown 导出。
2. 任务创建阶段的 FETCHING_PR、PARSING_DIFF 状态落地，避免进度流只停留在 PENDING/REVIEWING。
3. 正式建库 SQL 与实体、测试 schema 对齐。
```

后续展示扩展能力：

```text
1. GET /api/skills
2. GET /api/review-tasks/{taskId}/skill-results
3. GET /api/review-tasks/{taskId}/trace
4. PARTIAL_SUCCESS 或失败文件统计展示。
5. 自动评论到 GitHub PR。
```

当前最推荐下一步：

```text
1. 补 GET /api/review-tasks/{taskId}/review-markdown。
2. 同步正式 SQL，保证新环境能直接建库运行。
3. 处理任务状态流和部分失败状态。
```
